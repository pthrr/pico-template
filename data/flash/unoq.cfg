# Arduino UNO Q flash config for Embassy firmware
# Flashes directly to 0x08000000 (overwrites Arduino bootloader)
reset_config srst_only srst_push_pull
init
reset
halt
flash info 0
flash write_image erase ${filename} 0x08000000 bin
reset
shutdown
